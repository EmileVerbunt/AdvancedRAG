"""Neo4j integration for visualising the MS GraphRAG knowledge graph.

The loader is opt-in (`pip install '.[neo4j]'`) and used only by demos. The
base extraction pipeline never imports anything from this package.
"""
